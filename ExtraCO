SELECT 
date(StartTime) as Logdate,count(distinct(FORMAT_TIMESTAMP('%Y-%m-%d %H:%M',StartTime)))/1440 as Availability 
FROM `vz-it-pr-jabv-aidplt-0.AIDSRE.TD_HEART_BEAT_INFO` 
group by  1
order by 1 desc
